# TES LEIXA v8.22 FIX import re

Corrige el NameError en `detected_acronyms()` añadiendo `import re` al `app.py`.

Mantiene banco v820/v821 con 10.713 preguntas, siglas y confusiones.

Subir a GitHub: `app.py`, `requirements.txt`, `docs/`, `scripts/`, `README_v822_FIX_IMPORT_RE.md`, `CHANGELOG_v822_FIX_IMPORT_RE.md`.

No reemplazar `user_store.json` ni `user_store (2).json`.
